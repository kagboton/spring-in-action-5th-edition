server:
  port: 9090

spring:
  datasource:
    url: jdbc:h2:mem:testdb;
  jpa:
    show-sql: true
  data:
    rest:
      base-path: /api

taco:
  orders:
    pageSize: 10

  discount:
    codes:
      abcdef: 10